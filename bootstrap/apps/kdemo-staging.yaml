apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: boot-kdemo-staging
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  project: default
  source:
    repoURL: https://github.com/YShimy/argocd-lab-apps.git
    targetRevision: main
    path: kustomize-demo/overlays/staging
  destination:
    server: https://kubernetes.default.svc
    namespace: boot-staging
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
